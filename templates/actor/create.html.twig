{% extends 'base.html.twig' %}
{% block title %}Research database -> create actor{% endblock %}
{% block body %}

        <h3>Create actor</h3>
    <ul>
        <li>
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </li>
    </ul>
{% endblock %}